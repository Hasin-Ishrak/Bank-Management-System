# ৳ Core Asset & Credit Capital Management Dashboard

A premium, highly-optimized enterprise financial dashboard frontend crafted with **Next.js**, **React**, and **Tailwind CSS**. Optimized for core ledger monitoring, automated credit applications, and state identity security layers, initialized with comprehensive fallback mechanisms and native support for the **Bangladeshi Taka (৳)** currency standard.

---

## 🚀 Key Architectural Pillars

- **Dynamic Alert Infrastructure:** Employs an absolute floating micro-alert node system (`fixed top-6 right-6`) providing contextual operation feedback with programmatic micro-glow shadows and auto-dismiss sequences.
- **High-Fidelity Skeleton Loaders:** Eradicates layout shifting entirely through predictive HTML element structures that mimic tabular data alignment and balance meters during asynchronous hydration states.
- **Localized Currency Processing Unit (৳):** Deep integrations with presentation layers to format financial entries uniformly across application structures using native BDT protocols.
- **Client Boundary Safety:** Enforces strict `'use client'` execution sandboxing to secure state variables, component lifecycles, and credential tokens.

---

## 📁 System Architecture Blueprint

```text
├── src/
│   ├── app/
│   │   ├── loans/
│   │   │   └── page.jsx          # Credit allocation and loan status tracing node
│   │   └── profile/
│   │       └── page.jsx          # Identity metrics, account balances, and security passkeys
│   ├── hooks/
│   │   └── useAuth.js            # Unified core identity state synchronization hook
│   ├── services/
│   │   ├── authService.js        # Credential updates and authentication layer API bridges
│   │   ├── accountService.js     # Balances, status codes, and financial ledger data services
│   │   └── reportService.js      # Loan indexing, approval pipelines, and balance sheet submissions
│   └── lib/
│       └── normalize.js          # Financial format engines (formatMoney, extractList, extractData)
```
### 2. User Node & Asset Metrics (`ProfilePage`)
*   **Liquid Ledger Balances:** Tracks real-time active registry metrics and queries available liquid wealth parameters dynamically.
*   **Cryptographic Access Refresh:** Features password updates with explicit text length validation mechanisms (**minimum of 6 characters**) and layout safety hooks ensuring target parity between duplicate passphrase input elements.

---

## 💻 Tech Stack & Dependencies

*   **Framework Node:** Next.js (App Router, Client Component Architecture)
*   **Presentation System:** Tailwind CSS
*   **Icons & Signals:** Unicode Glyphs (✓, ✕, ৳)
*   **State Engines:** React Hook System (`useState`, `useEffect`, `useContext`)

---

## ⚙️ Installation & Development Environment Setup

### 2. Dependency Resolution
Pull active environment packages through configured node dependency managers:
```bash
npm install

### 1. Project Initialization
Clone the repository configuration file down to local system nodes and switch to directory roots:
```bash
git clone https://github.com/Hasin-Ishrak/Bank-Management-System.git
cd bdt-asset-dashboard
